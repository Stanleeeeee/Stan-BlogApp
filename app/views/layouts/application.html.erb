<!DOCTYPE html>
<html>
  <head>
    <title>Blog App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/b331332bf9.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </head>

  <body>
    <nav class="sticky-top navbar-dark bg-dark">
      <ul class="nav-list d-flex align-items-center justify-content-between mb-0">
        <li class="navbar-brand"> <%= link_to "👤 Blog", root_path, class: "home-blog" %> </li>
        <div class="d-flex align-items-center justify-content-between">
          <% if user_signed_in? %>
            <li class="navbar-brand fs-6"> Welcome <span style="color:var(--dark)"><%= current_user.name.capitalize %>!</span></li>
            <li class="navbar-brand fs-6"> <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary " %></li>
          <% else %>
            <li class="navbar-brand fs-6"> <%= link_to "Sign In", new_user_session_path, method: :delete, class: "btn btn-outline-secondary " %></li>
            <li class="navbar-brand fs-6"> <%= link_to "Sign Up", new_user_registration_path, method: :delete, class: "btn btn-outline-secondary " %></li>
          <% end %>
        </div>
      </ul>
    </nav>
     <% flash.each do |name, msg| -%>
      <div class="alert alert-success" role="alert">
        <%= msg %>
      </div>
    <% end -%>
    <div class="container" style="padding-bottom:50px">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
     <footer class="mt-4 d-flex justify-content-center fixed-bottom navbar navbar-light bg-light">
      <span class="fs-6 my-2">Designer: @Stanley</span>
    </footer>
  </body>
</html>

