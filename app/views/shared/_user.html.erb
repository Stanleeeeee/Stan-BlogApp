<%= stylesheet_link_tag('user') %>
<section class="user">
<div class="img-div">
<%= image_tag('user.png', class:'user_profile') %>
</div>
  <div class="content">
    <h2><%= link_to(user.name, user_path(user)) %></h2>
    <p>
      Number of posts: <%= user.posts_counter %>
    </p>
  </div>
</section>
